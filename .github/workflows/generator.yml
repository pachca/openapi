name: Generator

on:
  push:
    branches: [main]
    paths:
      - "packages/generator/**"
      - "packages/openapi-parser/**"
      - "packages/spec/openapi.yaml"
      - ".github/workflows/generator.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/generator/**"
      - "packages/openapi-parser/**"
      - "packages/spec/openapi.yaml"
      - ".github/workflows/generator.yml"

concurrency:
  group: generator-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  snapshot-tests:
    name: Snapshot tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build parser
        working-directory: packages/openapi-parser
        run: bun run build

      - name: Run snapshot tests
        working-directory: packages/generator
        run: bun test

  check-changes:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.check.outputs.should_publish }}
      version: ${{ steps.check.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20.20.2
      - name: Check generator release (changelog-driven)
        id: check
        run: node scripts/check-release.mjs --product generator --npm @pachca/generator --dir "packages/generator,packages/openapi-parser" >> "$GITHUB_OUTPUT"

  publish-generator:
    if: github.event_name == 'push' && needs.check-changes.outputs.should_publish == 'true'
    needs: [snapshot-tests, check-changes]
    runs-on: ubuntu-latest
    concurrency:
      group: publish-generator
      cancel-in-progress: false
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.4
      # setup-node v7 is what makes trusted publishing work: earlier versions
      # write an empty `_authToken` line into .npmrc whenever registry-url is
      # set and NODE_AUTH_TOKEN is not, and npm then treats auth as configured
      # and never starts the OIDC exchange. Node 24 ships npm 11.19, above the
      # 11.5.1 trusted publishing needs, so npm is not upgraded by hand.
      - uses: actions/setup-node@v7
        with:
          node-version: 24.21.0
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false
      - run: bun install --frozen-lockfile

      - name: Build parser
        working-directory: packages/openapi-parser
        run: bun run build

      - name: Build generator
        working-directory: packages/generator
        run: bun run build

      - name: Set version (from releases.json)
        working-directory: packages/generator
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          node -e "const fs=require('fs'),p=JSON.parse(fs.readFileSync('package.json'));p.version='$VERSION';fs.writeFileSync('package.json',JSON.stringify(p,null,2)+'\n')"
          echo "Publishing @pachca/generator@$VERSION"

      # Authenticated via OIDC (trusted publisher configured on npmjs), not a
      # token. Provenance is attached automatically, so --provenance is dropped.
      - name: Publish to npm (idempotent)
        working-directory: packages/generator
        run: |
          VERSION="${{ needs.check-changes.outputs.version }}"
          if npm view "@pachca/generator@${VERSION}" version 2>/dev/null; then
            echo "@pachca/generator@${VERSION} already published, skipping"
          else
            npm publish --access public
          fi
