---
title: Права и роли
description: "Как устроены права в API Пачки: роль в пространстве (Владелец, Администратор, Сотрудник, гости) и роль в чате (Создатель, Админ, Редактор, Участник, Подписчик), единственная точка их связи, видимость закрытых бесед и каналов, требования частых методов"
related:
  - { path: /api/authorization, title: Авторизация }
  - { path: /guides/bots/access, title: Доступы к чатам и сообщениям }
  - /guides/threads
---

# Права и роли

У API нет собственной логики доступа. Он работает по тем же правилам, что и интерфейс Пачки: токен видит ровно то, что видит выдавший его сотрудник, и может ровно то же, что может он. Если сотрудник не состоит в закрытом канале, то и его токен не получит оттуда ни сообщений, ни списка участников.

[Скоупы](/api/authorization#skoupy) работают поверх этих правил и только сужают доступ. Скоуп открывает методы, но не расширяет видимость: с `chats:read` вы получите те же чаты, что видны в интерфейсе, и ни одним больше.

## Две системы ролей

Роли в Пачке живут на двух независимых уровнях:

- **Роль в пространстве** — кем сотрудник является для всей организации.
- **Роль в чате** — кем он является внутри конкретной беседы или канала.

Уровни не наследуют права друг у друга. Администратор пространства не получает автоматического доступа к чужим чатам: в закрытом канале, где он не состоит, он такой же посторонний, как любой другой сотрудник. И наоборот, обычный сотрудник может быть Создателем канала и управлять там всем, включая администраторов пространства.

<Info>Различать эти уровни важнее всего при отладке `403`. Прежде чем искать причину в скоупах токена, проверьте, состоит ли выдавший его сотрудник в чате и с какой ролью.</Info>

## Роли в пространстве

Роль приходит в поле `role` объекта сотрудника и меняется методом [Редактирование сотрудника](PUT /users/{id}).

| Роль | Значение `role` | Что даёт |
|---|---|---|
| Владелец | `admin` | Всё, что может Администратор, плюс экспорт сообщений и журнал аудита. В пространстве только один. |
| Администратор | `admin` | Управление сотрудниками и тегами, настройки пространства |
| Сотрудник | `user` | Обычная работа с чатами, сообщениями, тредами и задачами |
| Мульти-гость | `multi_guest` | Доступ только к тем чатам, куда его добавили |
| Гость | `guest` | Доступ к одному чату, в который его пригласили |

Две особенности, из-за которых чаще всего возникают вопросы.

**Владелец не выделен отдельным значением роли.** В поле `role` у него `admin`, как и у остальных администраторов, и определить Владельца по объекту сотрудника нельзя. Его роль нельзя сменить ни на какую другую, кроме `admin`, а самого Владельца нельзя деактивировать.

**Гости и мульти-гости не работают с API.** Ни один скоуп не доступен этим ролям, поэтому создать токен они не могут. Это относится только к их собственным токенам: сами гости остаются полноценными участниками чатов, куда их добавили, и другие токены видят их как обычных участников.

### Что доступно только Владельцу и Администратору

Большинство скоупов доступно всем ролям, кроме гостевых. Исключения стоит знать заранее, потому что при создании токена такие скоупы просто не появятся в списке:

- **Только Владелец** — `chat_exports:read`, `chat_exports:write` для [экспорта сообщений](/guides/export) и `audit_events:read` для [журнала аудита](/guides/audit-events). Обе возможности доступны на тарифе **Корпорация**.
- **Владелец и Администратор** — `users:create`, `users:update`, `users:delete`, `user_status:read`, `user_status:write`, `user_avatar:write`, `group_tags:read`, `group_tags:write`.

Полная таблица скоупов по ролям — в разделе [Авторизация](/api/authorization#dostupnye-skoupy).

## Роли в чате

Набор ролей зависит от типа чата:

- **Беседа** — Создатель, Админ, Участник.
- **Канал** — Создатель, Админ, Редактор, Подписчик.

Роль приходит в поле `role` участника и меняется методом [Редактирование роли](PUT /chats/{id}/members/{user_id}). Роль Создателя изменить нельзя, у него всегда права Админа чата.

| Действие | Кто может |
|---|---|
| Читать сообщения и ставить реакции | Все участники чата |
| Создавать треды | Все участники чата |
| Писать сообщения | Создатель, Админ, Редактор, Участник |
| Закреплять сообщения | Создатель, Админ, Редактор, Участник |
| Добавлять участников | Все участники чата, кроме гостей и мульти-гостей |
| Удалять чужие сообщения | Создатель, Админ, Редактор |
| Исключать участников | Создатель, Админ |
| Менять роли участников | Создатель, Админ |
| Менять название и изображение чата | Создатель, Админ |
| Архивировать чат | Создатель, Админ |

Подписчик канала читает сообщения и ставит реакции, но не пишет. Этим он отличается от Участника беседы, хотя в API у обоих значение `role` равно `member`.

В открытой беседе или канале читать сообщения и ставить реакции может любой сотрудник пространства, не вступая в чат. Остальные действия из таблицы требуют участия.

<Info>В архивном чате недоступны отправка сообщений, управление участниками и изменение настроек — это ограничение действует для всех ролей, включая Создателя. Чтение, реакции и восстановление из архива остаются доступны.</Info>

## Как роль в пространстве влияет на роль в чате

Связь между двумя системами ровно одна: роль в чате назначается в момент добавления участника, и в этот момент учитывается его роль в пространстве.

| Кого добавляют | Роль в чате |
|---|---|
| Создателя чата | Создатель |
| Владельца или Администратора пространства | Админ |
| Бота в канал | Редактор |
| Остальных сотрудников | Участник, в канале Подписчик |

В личных сообщениях оба собеседника получают права Админа.

Дальше эта роль живёт самостоятельно и **не пересчитывается при смене роли в пространстве**. Отсюда два следствия, которые заметны через API:

- Сотрудника повысили до Администратора пространства — в чатах, где он уже состоял, он остаётся Участником.
- Администратора понизили до Сотрудника — в своих чатах он остаётся Админом, пока роль не изменят методом [Редактирование роли](PUT /chats/{id}/members/{user_id}).

Поэтому по роли в пространстве нельзя предсказать права в конкретном чате. Актуальную роль возвращает [Список участников чата](GET /chats/{id}/members).

## Видимость чатов

Видимость определяется участием, а не ролью в пространстве:

- **Открытые беседы и каналы** — видны всем сотрудникам пространства, даже если они не состоят в них.
- **Закрытые беседы и каналы** — видны только участникам. Это правило действует для всех ролей, включая Администратора и Владельца.
- **Личные сообщения** — доступны только собеседникам.
- **Треды** — доступны участникам родительского чата, а также тем, кого добавили в сам тред. Подробнее — в разделе [Треды](/guides/threads).

Единственное исключение сделано для Владельца пространства: он может получить состав участников любого чата методом [Список участников чата](GET /chats/{id}/members). На сообщения это исключение не распространяется, и содержимое закрытых чатов через этот метод не раскрывается.

Гости и мульти-гости видят только те чаты, в которые их добавили. Открытые чаты пространства им недоступны.

## Требования частых методов

| Что нужно сделать | Метод | Что требуется |
|---|---|---|
| Получить информацию о чате | [Информация о чате](GET /chats/{id}) | Участие в чате. Для открытых бесед и каналов достаточно быть сотрудником пространства. |
| Получить участников чата | [Список участников чата](GET /chats/{id}/members) | Участие в чате, либо открытый чат, либо роль Владельца пространства |
| Добавить участника | [Добавление участника в чат](POST /chats/{id}/members) | Участие в чате. Гости и мульти-гости добавлять не могут. |
| Изменить роль участника | [Редактирование роли](PUT /chats/{id}/members/{user_id}) | Роль Создателя или Админа в этом чате |
| Исключить участника | [Исключение участника](DELETE /chats/{id}/members/{user_id}) | Роль Создателя или Админа в этом чате. Исключить Создателя нельзя. |
| Отправить сообщение | [Новое сообщение](POST /messages) | Право писать в этом чате. Подписчик канала писать не может. |
| Создать сотрудника | [Новый сотрудник](POST /users) | Роль Администратора или Владельца пространства |
| Экспортировать сообщения | [Создание экспорта](POST /chats/exports) | Роль Владельца пространства, тариф **Корпорация** |
| Прочитать журнал аудита | [Список событий аудита](GET /audit_events) | Роль Владельца пространства, тариф **Корпорация** |

## Личные данные сотрудников

Поля `email` и `phone_number` в объекте сотрудника выдаются не всем:

- **Владелец и Администратор** видят их всегда.
- **Сотрудник** видит их, если в настройках пространства разрешён показ личных данных. Иначе оба поля приходят как `null`, кроме собственного профиля.
- **Токен бота** подчиняется той же настройке пространства.

Собственные `email` и `phone_number` доступны всегда, независимо от роли и настройки.

## Ошибки доступа

- `403 Forbidden` — прав недостаточно. Либо у токена нет нужного скоупа (поле `error` содержит `insufficient_scope`), либо у его владельца нет права на это действие: он не состоит в чате или его роль в чате ниже требуемой.
- `404 Not Found` — объекта не существует. Если чат существует, но недоступен, вернётся `403`.

Подробнее о структуре ответов с ошибками — в разделе [Ошибки](/api/errors).
