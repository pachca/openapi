> Это Markdown-версия конкретной страницы. Для контекста за её пределами (правила API, полный перечень методов, авторизация) ОБЯЗАТЕЛЬНО открой [llms.txt](https://dev.pachca.com/llms.txt) перед ответом — это сэкономит токены и предотвратит неполный ответ.

# Суточный лимит сообщений, отключение 2FA в журнале аудита и сроки ссылок на файлы

_28 августа 2026_

У отправки появился суточный предел — 5 000 сообщений в один чат за скользящие
24 часа, сообщения в тред считаются в чат родительского. Рядом действует общий
предел по всем чатам сразу — 30 запросов за 5 секунд. Отказ приходит с `429`, и
у него два вида: суточный предел отдаёт обычную ошибку, а лимит на частоту
запросов — текст, а не `JSON`.

У кнопок описаны пределы: до 8 в строке, не больше 32 строк и 100 на сообщение.
Размер файла `size` описан 64-битным числом, а ссылка на вложение живёт до
7 дней. Удаление сообщения стирает вложения из хранилища сразу.

Подробнее — в разделах [Лимиты](/api/limits), [Ошибки](/api/errors),
[Кнопки](/guides/buttons) и [Загрузка файлов](/api/file-uploads).

- [Новое сообщение](/api/messages/create)
- [Редактирование сообщения](/api/messages/update)
- [Информация о сообщении](/api/messages/get)

Ответ `409` описан у методов, где он возможен, и называет конфликтующее поле в
`errors[].key` — например, `email` при повторном адресе почты сотрудника.

- [Новый сотрудник](/api/users/create)
- [Редактирование сотрудника](/api/users/update)
- [Новый тег](/api/group-tags/create)
- [Редактирование тега](/api/group-tags/update)
- [Закрепить сообщение](/api/messages/pin)

Журнал аудита пополнился событием `user_2fa_disabled` — сотрудник отключил себе
двухфакторную аутентификацию.

- [Журнал аудита событий](/api/security/list)

Ссылка на файл из формы живёт 2 часа, а сам файл хранится сутки после отправки —
[Обработка форм](/guides/forms/handling). Вложенный `.html` Пачка показывает
просмотром прямо в переписке: скрипты не выполняются, картинки и стили нужно
вшивать в документ — [Оформление ответов](/guides/ai-agents/markdown).

В поле `content` исходящего вебхука приходят цитаты и списки, а подчёркивание не
отражается. Описания [блоков формы](/guides/forms/blocks), напоминаний и
[разметки](/guides/markdown) сверены с поведением API. На странице
[Лимиты](/api/limits) описан отдельный предел для выборки списка чатов номером
страницы.

### n8n Node v2.1.1

- `Security`: фильтр событий пополнился значением `User 2fa Disabled` — сотрудник отключил себе двухфакторную аутентификацию

### CLI v2026.8.3

- `pachca security list` — флаг `--event-key` принимает значение `user_2fa_disabled`
- `pachca api POST /messages --spec` — размер файла `size` описан 64-битным числом, у ссылки `url` указан срок действия до 7 дней, а у кнопок появились пределы: `url` до 1024 символов и не больше 32 строк в `buttons`
- `pachca api POST /views/open --spec` — у радиокнопки блока `radio` изначальный выбор задаётся полем `checked`, как у чекбоксов, а не `selected`. У пункта списка предел `value` — 255 символов, а сам `options` стал обязательным
- `pachca api POST /tasks --spec` и `pachca api PUT /tasks/{id} --spec` — у `priority` показаны допустимые границы 1–3, которые проверяет API
- `pachca api --spec` показывает ответ `409` у методов сотрудника, тега и закрепления сообщения и ответ `429` у `POST /messages` — суточный предел сообщений в чат
- `pachca api GET /tasks --spec` — у фильтров `chat_ids` и `performer_ids` показан предел в 100 идентификаторов, который проверяет API

### SDK v1.4.0

- Поле `size` у файла сообщения и у записи видеозвонка описано 64-битным числом — размер файла может превышать 2 ГБ. Методы `Новое сообщение` (`POST /messages`), `Редактирование сообщения` (`PUT /messages/{id}`), payload вебхука о видеозвонке
- Поле `selected` у радиокнопки блока `radio` переименовано в `checked` — так его называет форма. Модель `ViewBlockSelectableOption`, метод `Открыть форму` (`POST /views/open`)
- Поле `options` у блоков `select`, `radio` и `checkbox` стало обязательным — форма без пунктов отклоняется валидацией
- Поле `description` у пункта выпадающего списка — модель `ViewBlockSelectOption`
- Значение `user_2fa_disabled` в перечислении типов аудит-событий
