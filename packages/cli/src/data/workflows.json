[
  {
    "title": "Найти чат по имени и отправить сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "GET /chats — перебери результаты, найди нужный по полю `name`",
        "command": "pachca api GET /chats"
      },
      {
        "description": "Отправь POST /messages с `\"entity_id\": chat.id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Отправить сообщение в канал или беседу (если chat_id известен)",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Отправь POST /messages с `\"entity_id\": chat_id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Отправить личное сообщение пользователю",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Определи `user_id` получателя (GET /users или из контекста)",
        "command": "pachca api GET /users"
      },
      {
        "description": "Отправь POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\": user_id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Ответить в тред (комментарий к сообщению)",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Получи или создай тред: POST /messages/{id}/thread (`id` — id родительского сообщения)",
        "command": "pachca api POST /messages/{id}/thread"
      },
      {
        "description": "Из ответа возьми id треда (`thread.id`)"
      },
      {
        "description": "Отправь POST /messages с `\"entity_type\": \"thread\"`, `\"entity_id\": thread.id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Ответить пользователю, который написал боту",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Вебхук содержит `entity_type` — он однозначно определяет контекст: `\"user\"` — личное сообщение боту, `\"thread\"` — сообщение в треде, `\"discussion\"` — сообщение в канале или беседе"
      },
      {
        "description": "DM (`entity_type: \"user\"`): ответь POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\"`: `user_id` из вебхука",
        "command": "pachca api POST /messages"
      },
      {
        "description": "Тред (`entity_type: \"thread\"`): вложенных тредов нет — ответь в тот же тред: POST /messages с `\"entity_type\": \"thread\"`, `\"entity_id\"`: `entity_id` из вебхука, `\"parent_message_id\"`: `id` сообщения пользователя из вебхука",
        "command": "pachca api POST /messages"
      },
      {
        "description": "Беседа/канал (`entity_type: \"discussion\"`): выбери стратегию — inline-ответ (POST /messages c `\"parent_message_id\"`: `id` сообщения) или тред (POST /messages/{id}/thread → ответ в тред)",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Отправить сообщение с файлами",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Для каждого файла: POST /uploads → получи `key` (с `${filename}`), `direct_url`, `policy`, подпись",
        "command": "pachca api POST /uploads"
      },
      {
        "description": "Для каждого файла: подставь имя файла вместо `${filename}` в `key`, затем загрузи файл POST на `direct_url` (`multipart/form-data`, без авторизации)"
      },
      {
        "description": "Собери массив `files` из всех загруженных файлов (`key`, `name`, `file_type`, `size`)"
      },
      {
        "description": "Отправь POST /messages с массивом `files` — одно сообщение со всеми файлами",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Отправить сообщение с кнопками",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Сформируй массив `buttons` — массив строк, каждая строка — массив кнопок: `[[{кнопка1, кнопка2}, ...], ...]`"
      },
      {
        "description": "Каждая кнопка: `{\"text\": \"Текст\"}` + либо `url` (ссылка), либо `data` (callback для вебхука)"
      },
      {
        "description": "Отправь POST /messages с полем `buttons`",
        "command": "pachca api POST /messages"
      },
      {
        "description": "Нажатия кнопок приходят в исходящий вебхук (событие \"Нажатие кнопок\")"
      }
    ]
  },
  {
    "title": "Получить историю сообщений чата",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "GET /messages?chat_id={id}",
        "command": "pachca api GET /messages?chat_id={id}"
      },
      {
        "description": "Пагинация: `limit` (1-50, по умолчанию 50) и `cursor` (из `meta.paginate.next_page`)"
      },
      {
        "description": "Сортировка: `sort[id]=asc` или `sort[id]=desc` (по умолчанию `\"desc\"`)"
      }
    ]
  },
  {
    "title": "Получить вложения из сообщения",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "GET /messages/{id} — в поле `files[]` каждый объект содержит `url` (прямая ссылка), `name`, `file_type`, `size`",
        "command": "pachca api GET /messages/{id}"
      },
      {
        "description": "Скачай нужные файлы по `files[].url` — ссылка прямая, авторизация не требуется"
      }
    ]
  },
  {
    "title": "Закрепить/открепить сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Закрепить: POST /messages/{id}/pin",
        "command": "pachca api POST /messages/{id}/pin"
      },
      {
        "description": "Открепить: DELETE /messages/{id}/pin",
        "command": "pachca api DELETE /messages/{id}/pin"
      }
    ]
  },
  {
    "title": "Подписаться на тред сообщения",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "POST /messages/{id}/thread — если треда нет, он будет создан; если есть — вернётся существующий",
        "command": "pachca api POST /messages/{id}/thread"
      },
      {
        "description": "Из ответа возьми `chat_id` треда (`data.chat_id`)"
      },
      {
        "description": "Добавь бота (или пользователя) в участники чата треда: POST /chats/{id}/members с `member_ids`",
        "command": "pachca api POST /chats/{id}/members"
      },
      {
        "description": "Теперь бот будет получать вебхук-события о новых сообщениях в этом треде"
      }
    ]
  },
  {
    "title": "Упомянуть пользователя по имени",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Определи поисковый запрос — используй фамилию, она уникальнее. Имена не склоняются в API, приводи к именительному падежу: «упомяни Пашу» → ищи `Паша` или `Павел`, «тегни Голубева» → ищи `Голубев`"
      },
      {
        "description": "Ищи сначала среди участников целевого чата: GET /chats/{id}/members (для треда тоже работает, у него свои участники) — фильтруй по имени на клиенте",
        "command": "pachca api GET /chats/{id}/members"
      },
      {
        "description": "Если пишешь в тред: также проверь участников родительского чата (GET /chats/{id}/members с `id=message_chat_id`)",
        "command": "pachca api GET /chats/{id}/members"
      },
      {
        "description": "Не нашёл — ищи по всей компании: GET /users?query={запрос}",
        "command": "pachca api GET /users?query={запрос}"
      },
      {
        "description": "Один подходящий результат → используй `nickname`. Несколько → уточни у пользователя (имя + фамилия). Ничего → попробуй другую форму имени (уменьшительное ↔ полное)"
      },
      {
        "description": "Вставь `@nickname` в текст сообщения"
      }
    ]
  },
  {
    "title": "Отредактировать сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "PUT /messages/{id} с полем `content` (и/или `buttons`, `files`)",
        "command": "pachca api PUT /messages/{id}"
      }
    ]
  },
  {
    "title": "Изменить вложения сообщения",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "GET /messages/{id} — получи текущие вложения из поля `files[]`, сохрани нужные объекты (`key`, `name`, `file_type`, `size`)",
        "command": "pachca api GET /messages/{id}"
      },
      {
        "description": "Если нужно добавить новый файл: POST /uploads → загрузи файл → добавь объект в список",
        "command": "pachca api POST /uploads"
      },
      {
        "description": "PUT /messages/{id} с массивом `files` — только те файлы, которые должны остаться (+ новые при необходимости)",
        "command": "pachca api PUT /messages/{id}"
      }
    ]
  },
  {
    "title": "Удалить сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "DELETE /messages/{id}",
        "command": "pachca api DELETE /messages/{id}"
      }
    ]
  },
  {
    "title": "Добавить реакцию на сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "POST /messages/{id}/reactions с полем `code` (emoji)",
        "command": "pachca api POST /messages/{id}/reactions"
      },
      {
        "description": "Убрать реакцию: DELETE /messages/{id}/reactions с полем `code`",
        "command": "pachca api DELETE /messages/{id}/reactions"
      }
    ]
  },
  {
    "title": "Проверить, кто прочитал сообщение",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "GET /messages/{id}/read_member_ids — возвращает массив `user_id` прочитавших",
        "command": "pachca api GET /messages/{id}/read_member_ids"
      },
      {
        "description": "При необходимости сопоставь с GET /users для получения имён",
        "command": "pachca api GET /users"
      }
    ]
  },
  {
    "title": "Разослать уведомление нескольким пользователям",
    "skill": "pachca-messages",
    "steps": [
      {
        "description": "Определи список `user_id` получателей (GET /users или из контекста)",
        "command": "pachca api GET /users"
      },
      {
        "description": "Для каждого: POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\": user_id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Создать канал и пригласить участников",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "POST /chats — `\"channel\": true` для канала, `false` (по умолчанию) для беседы",
        "command": "pachca api POST /chats"
      },
      {
        "description": "Участников можно передать сразу при создании: `member_ids` и/или `group_tag_ids` в теле запроса"
      },
      {
        "description": "Или добавить позже: POST /chats/{id}/members с `member_ids`, POST /chats/{id}/group_tags с `group_tag_ids`",
        "command": "pachca api POST /chats/{id}/members"
      }
    ]
  },
  {
    "title": "Архивация и управление чатом",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "Архивировать: PUT /chats/{id}/archive",
        "command": "pachca api PUT /chats/{id}/archive"
      },
      {
        "description": "Разархивировать: PUT /chats/{id}/unarchive",
        "command": "pachca api PUT /chats/{id}/unarchive"
      },
      {
        "description": "Изменить роль участника: PUT /chats/{id}/members/{user_id} с `role` (`\"admin\"` | `\"member\"`; `\"editor\"` — только для каналов). Роль создателя чата изменить нельзя.",
        "command": "pachca api PUT /chats/{id}/members/{user_id}"
      },
      {
        "description": "Удалить участника: DELETE /chats/{id}/members/{user_id}",
        "command": "pachca api DELETE /chats/{id}/members/{user_id}"
      },
      {
        "description": "Покинуть чат: DELETE /chats/{id}/leave",
        "command": "pachca api DELETE /chats/{id}/leave"
      }
    ]
  },
  {
    "title": "Переименовать или обновить чат",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "PUT /chats/{id} с нужными параметрами: `name` (название) и/или `public` (открытый доступ)",
        "command": "pachca api PUT /chats/{id}"
      }
    ]
  },
  {
    "title": "Создать проектную беседу из шаблона",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "POST /chats с `name`, `\"channel\": false` и `group_tag_ids` (добавить всех участников тега сразу)",
        "command": "pachca api POST /chats"
      },
      {
        "description": "Или POST /chats → затем POST /chats/{id}/members с `member_ids` + POST /chats/{id}/group_tags с `group_tag_ids`",
        "command": "pachca api POST /chats"
      },
      {
        "description": "Отправь приветственное сообщение: POST /messages с `\"entity_id\": chat.id`",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Экспорт истории чата",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "POST /chats/exports с `start_at`, `end_at` (формат YYYY-MM-DD) и обязательным `webhook_url` — запрос выполняется асинхронно",
        "command": "pachca api POST /chats/exports"
      },
      {
        "description": "Дождись вебхука на `webhook_url`: придёт JSON с `\"type\": \"export\"`, `\"event\": \"ready\"` и полем `export_id` — по `\"type\": \"export\"` можно отличить от других вебхуков"
      },
      {
        "description": "GET /chats/exports/{id} — сервер вернёт 302, большинство HTTP-клиентов скачают файл автоматически",
        "command": "pachca api GET /chats/exports/{id}"
      }
    ]
  },
  {
    "title": "Найти активные чаты за период",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "GET /chats с `last_message_at_after={дата}` — только чаты с активностью после указанной даты",
        "command": "pachca api GET /chats"
      },
      {
        "description": "Для диапазона добавь `last_message_at_before={дата}` — чаты с активностью между двумя датами"
      },
      {
        "description": "Перебери страницы: `cursor` из `meta.paginate.next_page`, пока он не пустой"
      }
    ]
  },
  {
    "title": "Найти и заархивировать неактивные чаты",
    "skill": "pachca-chats",
    "steps": [
      {
        "description": "GET /chats с `last_message_at_before={порог}` — сразу только чаты без активности с нужной даты",
        "command": "pachca api GET /chats"
      },
      {
        "description": "Перебери страницы: `cursor` из `meta.paginate.next_page`, пока он не пустой"
      },
      {
        "description": "Для каждого чата: PUT /chats/{id}/archive",
        "command": "pachca api PUT /chats/{id}/archive"
      }
    ]
  },
  {
    "title": "Настроить бота с исходящим вебхуком",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "Создай бота в интерфейсе Пачки: Автоматизации → Интеграции → Webhook"
      },
      {
        "description": "Получи `access_token` бота во вкладке «API» настроек бота"
      },
      {
        "description": "Укажи Webhook URL для получения событий"
      },
      {
        "description": "Выбери типы событий: новые сообщения, реакции, кнопки, участники"
      }
    ]
  },
  {
    "title": "Обновить Webhook URL бота",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "PUT /bots/{id} с новым `outgoing_url` — `id` бота (его `user_id`) можно узнать во вкладке «API» настроек бота",
        "command": "pachca api PUT /bots/{id}"
      }
    ]
  },
  {
    "title": "Обработать входящий вебхук-событие",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "Получи POST-запрос на свой Webhook URL"
      },
      {
        "description": "Проверь подпись (Signing secret) для безопасности"
      },
      {
        "description": "Проверь `webhook_timestamp` — должен быть в пределах 1 минуты"
      },
      {
        "description": "Разбери JSON: тип события, данные"
      },
      {
        "description": "Для полной информации сделай запрос к API: GET /messages/{id} — особенно важно для получения вложений (`files[]`), которых нет в вебхуке",
        "command": "pachca api GET /messages/{id}"
      }
    ]
  },
  {
    "title": "Разворачивание ссылок (unfurling)",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "Создай специального Unfurl-бота и укажи отслеживаемые домены в его настройках"
      },
      {
        "description": "При появлении ссылки бот получает вебхук `\"event\": \"link_shared\"` с массивом `links` (`url` + `domain`) и `message_id`"
      },
      {
        "description": "Извлеки данные из своей системы по URL из `links`"
      },
      {
        "description": "Отправь POST /messages/{id}/link_previews с превью-данными",
        "command": "pachca api POST /messages/{id}/link_previews"
      }
    ]
  },
  {
    "title": "Обработать нажатие кнопки (callback)",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "Получи вебхук с `\"event\": \"message_button_clicked\"` — в payload: `data` (из кнопки), `user_id`, `message_id`"
      },
      {
        "description": "Выполни нужное действие (запись в БД, запрос к API и т.д.)"
      },
      {
        "description": "Ответь пользователю: POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\": user_id` из вебхука",
        "command": "pachca api POST /messages"
      },
      {
        "description": "Опционально: обнови исходное сообщение через PUT /messages/{id} — чтобы убрать кнопки передай `\"buttons\": []`, чтобы изменить текст — передай `\"content\"`",
        "command": "pachca api PUT /messages/{id}"
      }
    ]
  },
  {
    "title": "Периодический дайджест/отчёт",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "По расписанию (cron/scheduler): собери данные из своей системы"
      },
      {
        "description": "Сформируй текст сообщения с нужными метриками или сводкой"
      },
      {
        "description": "POST /messages с `\"entity_id\": chat_id` нужного канала",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Мониторинг и алерты",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "Внешняя система (CI, мониторинг, сервис) обнаруживает событие (ошибка, деплой, порог метрики)"
      },
      {
        "description": "Делает POST запрос к твоему боту или напрямую вызывает Pachca API"
      },
      {
        "description": "POST /messages в канал алертов с описанием события и кнопками «Взять в работу» / «Игнорировать»",
        "command": "pachca api POST /messages"
      },
      {
        "description": "При нажатии кнопки — обработай callback и обнови статус алерта"
      }
    ]
  },
  {
    "title": "Обработка событий через историю (polling)",
    "skill": "pachca-bots",
    "steps": [
      {
        "description": "В настройках бота включи «Сохранять историю событий» (вкладка «Исходящий webhook»). Webhook URL указывать не обязательно."
      },
      {
        "description": "По расписанию или по запросу: GET /webhooks/events — получи накопленные события с пагинацией (`limit`, `cursor`)",
        "command": "pachca api GET /webhooks/events"
      },
      {
        "description": "Обработай каждое событие (тот же формат payload, что и в real-time вебхуке)"
      },
      {
        "description": "После обработки: DELETE /webhooks/events/{id} — удали событие, чтобы не обработать повторно",
        "command": "pachca api DELETE /webhooks/events/{id}"
      }
    ]
  },
  {
    "title": "Показать интерактивную форму пользователю",
    "skill": "pachca-forms",
    "steps": [
      {
        "description": "Заранее подготовь объект формы: `view` с `title`, `blocks` (типы: `input`, `select`, `radio`, `checkbox`, `date`, `time`, `file_input`, `header`, `plain_text`, `markdown`, `divider`), опционально `callback_id` (идентификатор формы) и `private_metadata` (контекст, например id сообщения)"
      },
      {
        "description": "Отправь сообщение с кнопкой (POST /messages с `buttons`, в `data` кнопки передай идентификатор формы)",
        "command": "pachca api POST /messages"
      },
      {
        "description": "При нажатии кнопки — получи вебхук-событие с `trigger_id`"
      },
      {
        "description": "Немедленно отправь POST /views/open с `trigger_id` и готовым объектом формы",
        "command": "pachca api POST /views/open"
      },
      {
        "description": "Пользователь заполняет форму → при отправке получи вебхук — обработай по сценарию «Обработать отправку формы»"
      }
    ]
  },
  {
    "title": "Обработать отправку формы (view_submission)",
    "skill": "pachca-forms",
    "steps": [
      {
        "description": "Получи вебхук с `\"type\": \"view\"`, `\"event\": \"submit\"` — содержит `callback_id`, `user_id`, `private_metadata` и `data` (значения полей, ключи совпадают с полем `name` каждого блока)"
      },
      {
        "description": "Извлеки значения из `data`: например, для блока с `\"name\": \"comment\"` значение в `data.comment`"
      },
      {
        "description": "Если форма содержит `file_input` — скачай файлы по `data.field_name[].url` немедленно: ссылки истекают через 1 час"
      },
      {
        "description": "Если данные валидны → ответь HTTP 200 (пустое тело) — форма закроется у пользователя"
      },
      {
        "description": "Если есть ошибки → ответь HTTP 400 с `{\"errors\": {\"field_name\": \"текст ошибки\"}}` — пользователь увидит ошибки в форме и сможет исправить и отправить повторно"
      }
    ]
  },
  {
    "title": "Опрос сотрудников через форму",
    "skill": "pachca-forms",
    "steps": [
      {
        "description": "Отправь сообщение с кнопкой «Пройти опрос» в канал или ЛС: POST /messages с `\"data\": \"survey_start\"` в кнопке",
        "command": "pachca api POST /messages"
      },
      {
        "description": "При нажатии кнопки получи вебхук с `trigger_id` и `user_id` нажавшего"
      },
      {
        "description": "Немедленно отправь POST /views/open с формой (поля: `input`, `select`, `radio` и т.д.)",
        "command": "pachca api POST /views/open"
      },
      {
        "description": "При отправке формы получи вебхук с `\"event\": \"submit\"` — значения полей в `data`"
      },
      {
        "description": "Обработай ответы: сохрани в базу или отправь итоговым сообщением в канал"
      },
      {
        "description": "Ответь HTTP 200 — форма закроется"
      }
    ]
  },
  {
    "title": "Форма заявки/запроса",
    "skill": "pachca-forms",
    "steps": [
      {
        "description": "Размести в канале сообщение с кнопкой «Создать заявку» (`\"data\": \"new_request\"`)"
      },
      {
        "description": "При нажатии открой форму с полями: тема, описание, приоритет (`select`)"
      },
      {
        "description": "При submit-вебхуке: создай задачу (POST /tasks) или отправь уведомление ответственному (POST /messages с `\"entity_type\": \"user\"`)",
        "command": "pachca api POST /tasks)"
      },
      {
        "description": "Отправь подтверждение автору: POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\": user_id` из вебхука",
        "command": "pachca api POST /messages"
      },
      {
        "description": "Ответь HTTP 200 — форма закроется"
      }
    ]
  },
  {
    "title": "Получить сотрудника по ID",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "GET /users/{id} — полная информация о сотруднике",
        "command": "pachca api GET /users/{id}"
      }
    ]
  },
  {
    "title": "Массовое создание сотрудников с тегами",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "Создай тег (если нужен): POST /group_tags с `{\"group_tag\": {\"name\": ...}}`",
        "command": "pachca api POST /group_tags"
      },
      {
        "description": "Для каждого сотрудника: POST /users — теги назначаются через поле `list_tags` в теле запроса",
        "command": "pachca api POST /users"
      },
      {
        "description": "Или обнови существующего: PUT /users/{id} с `list_tags`",
        "command": "pachca api PUT /users/{id}"
      }
    ]
  },
  {
    "title": "Найти сотрудника по имени или email",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "GET /users?query=Иван — поиск по имени/email (частичное совпадение)",
        "command": "pachca api GET /users?query=Иван"
      },
      {
        "description": "Если нужен точный поиск по email — перебери страницы и отфильтруй на клиенте"
      }
    ]
  },
  {
    "title": "Онбординг нового сотрудника",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "POST /users с `email`, именем, тегами (`list_tags`) — создать аккаунт",
        "command": "pachca api POST /users"
      },
      {
        "description": "POST /chats/{id}/members с `member_ids` — добавить в нужные каналы (онбординг, общий, тематические)",
        "command": "pachca api POST /chats/{id}/members"
      },
      {
        "description": "POST /messages с `\"entity_type\": \"user\"`, `\"entity_id\": user.id` — отправить welcome-сообщение в личные сообщения",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Offboarding сотрудника",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "PUT /users/{id} с `\"suspended\": true` — заблокировать доступ",
        "command": "pachca api PUT /users/{id}"
      },
      {
        "description": "Опционально: DELETE /users/{id} — удалить аккаунт полностью",
        "command": "pachca api DELETE /users/{id}"
      }
    ]
  },
  {
    "title": "Получить всех сотрудников тега/департамента",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "GET /group_tags?names[]=Backend — найти тег по названию",
        "command": "pachca api GET /group_tags?names[]=Backend"
      },
      {
        "description": "Из ответа взять `id` тега"
      },
      {
        "description": "GET /group_tags/{id}/users с пагинацией (`limit` + `cursor`) — получить всех участников",
        "command": "pachca api GET /group_tags/{id}/users"
      }
    ]
  },
  {
    "title": "Управление статусом сотрудника",
    "skill": "pachca-users",
    "steps": [
      {
        "description": "GET /users/{user_id}/status — получить текущий статус сотрудника",
        "command": "pachca api GET /users/{user_id}/status"
      },
      {
        "description": "PUT /users/{user_id}/status с `emoji`, `title` и опционально `is_away: true`, `away_message: \"текст\"` — установить статус",
        "command": "pachca api PUT /users/{user_id}/status"
      },
      {
        "description": "DELETE /users/{user_id}/status — удалить статус сотрудника",
        "command": "pachca api DELETE /users/{user_id}/status"
      }
    ]
  },
  {
    "title": "Создать напоминание",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "POST /tasks с `kind`, `content` и `due_at`",
        "command": "pachca api POST /tasks"
      },
      {
        "description": "Чтобы привязать к чату — добавь `chat_id`"
      },
      {
        "description": "Чтобы заполнить дополнительные поля — добавь `custom_properties: [{\"id\": <field_id>, \"value\": \"...\"}]` (список полей: GET /custom_properties?entity_type=Task)",
        "command": "pachca api GET /custom_properties?entity_type=Task)"
      }
    ]
  },
  {
    "title": "Получить список предстоящих задач",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "GET /tasks с пагинацией (`limit`, `cursor`)",
        "command": "pachca api GET /tasks"
      },
      {
        "description": "Отфильтруй на клиенте по полю `status`: `\"undone\"` — не выполнена, `\"done\"` — выполнена"
      }
    ]
  },
  {
    "title": "Получить задачу по ID",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "GET /tasks/{id} — полная информация о задаче, включая `custom_properties`, `performer_ids`, `status`",
        "command": "pachca api GET /tasks/{id}"
      }
    ]
  },
  {
    "title": "Отметить задачу выполненной",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "PUT /tasks/{id} с `\"status\": \"done\"`",
        "command": "pachca api PUT /tasks/{id}"
      }
    ]
  },
  {
    "title": "Обновить задачу (перенести срок, сменить ответственных)",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "PUT /tasks/{id} с нужными полями: `content`, `due_at`, `kind`, `priority`, `performer_ids`, `custom_properties`",
        "command": "pachca api PUT /tasks/{id}"
      }
    ]
  },
  {
    "title": "Удалить задачу",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "DELETE /tasks/{id}",
        "command": "pachca api DELETE /tasks/{id}"
      }
    ]
  },
  {
    "title": "Создать серию напоминаний",
    "skill": "pachca-tasks",
    "steps": [
      {
        "description": "Подготовь список дат (ежедневно, еженедельно и т.д.)"
      },
      {
        "description": "Для каждой даты: POST /tasks с нужным `kind`, `content` и `due_at`",
        "command": "pachca api POST /tasks"
      }
    ]
  },
  {
    "title": "Получить свой профиль",
    "skill": "pachca-profile",
    "steps": [
      {
        "description": "GET /profile — возвращает полную информацию о текущем пользователе",
        "command": "pachca api GET /profile"
      }
    ]
  },
  {
    "title": "Проверить свой токен",
    "skill": "pachca-profile",
    "steps": [
      {
        "description": "GET /oauth/token/info — возвращает информацию о текущем токене: скоупы, дату создания, срок жизни",
        "command": "pachca api GET /oauth/token/info"
      }
    ]
  },
  {
    "title": "Установить статус",
    "skill": "pachca-profile",
    "steps": [
      {
        "description": "PUT /profile/status с `emoji` и `title`",
        "command": "pachca api PUT /profile/status"
      },
      {
        "description": "Чтобы включить режим «Нет на месте» — добавь `is_away: true`"
      },
      {
        "description": "Чтобы задать сообщение о недоступности — добавь `away_message: \"текст\"` (макс 1024 символа, отображается в профиле и при личных сообщениях/упоминаниях)"
      },
      {
        "description": "Чтобы статус автоматически сбросился — добавь `expires_at: \"2024-04-08T10:00:00.000Z\"` (ISO-8601, UTC+0)"
      }
    ]
  },
  {
    "title": "Сбросить статус",
    "skill": "pachca-profile",
    "steps": [
      {
        "description": "DELETE /profile/status",
        "command": "pachca api DELETE /profile/status"
      }
    ]
  },
  {
    "title": "Получить кастомные поля профиля",
    "skill": "pachca-profile",
    "steps": [
      {
        "description": "GET /custom_properties?entity_type=User — список дополнительных полей для сотрудников (`id`, `name`, `data_type`)",
        "command": "pachca api GET /custom_properties?entity_type=User"
      },
      {
        "description": "GET /profile — в ответе поле `custom_properties` содержит значения для текущего пользователя",
        "command": "pachca api GET /profile"
      }
    ]
  },
  {
    "title": "Найти сообщение по тексту",
    "skill": "pachca-search",
    "steps": [
      {
        "description": "GET /search/messages?query=текст — полнотекстовый поиск",
        "command": "pachca api GET /search/messages?query=текст"
      },
      {
        "description": "Пагинация: `limit` (до 200) и `cursor` (из `meta.paginate.next_page`)"
      },
      {
        "description": "Общее количество результатов — в `meta.total`"
      }
    ]
  },
  {
    "title": "Найти чат по названию",
    "skill": "pachca-search",
    "steps": [
      {
        "description": "GET /search/chats?query=название — полнотекстовый поиск по чатам",
        "command": "pachca api GET /search/chats?query=название"
      },
      {
        "description": "Пагинация: `limit` (до 100) и `cursor`"
      }
    ]
  },
  {
    "title": "Найти сотрудника по имени",
    "skill": "pachca-search",
    "steps": [
      {
        "description": "GET /search/users?query=имя — полнотекстовый поиск по сотрудникам",
        "command": "pachca api GET /search/users?query=имя"
      },
      {
        "description": "Пагинация: `limit` (до 200) и `cursor`"
      },
      {
        "description": "Сортировка: `sort=alphabetical` для алфавитного порядка, `sort=by_score` (по умолчанию) для релевантности"
      }
    ]
  },
  {
    "title": "Получить журнал аудита событий",
    "skill": "pachca-security",
    "steps": [
      {
        "description": "GET /audit_events с обязательными `start_time` и `end_time` (ISO-8601, UTC+0)",
        "command": "pachca api GET /audit_events"
      },
      {
        "description": "Опциональные фильтры: `event_key`, `actor_id`, `actor_type`, `entity_id`, `entity_type`"
      }
    ]
  },
  {
    "title": "Мониторинг подозрительных входов",
    "skill": "pachca-security",
    "steps": [
      {
        "description": "GET /audit_events с `event_key=user_2fa_fail` (или `user_login`) за нужный период",
        "command": "pachca api GET /audit_events"
      },
      {
        "description": "Пагинируй с `cursor` до получения всех записей"
      },
      {
        "description": "Если найдены аномалии (много неудачных 2FA с одного аккаунта) — отправь уведомление администратору через POST /messages",
        "command": "pachca api POST /messages"
      }
    ]
  },
  {
    "title": "Экспорт логов за период",
    "skill": "pachca-security",
    "steps": [
      {
        "description": "GET /audit_events с `start_time` и `end_time` (ISO-8601, UTC+0)",
        "command": "pachca api GET /audit_events"
      },
      {
        "description": "Пагинируй с `cursor` до получения всех записей (`limit` до 50)"
      },
      {
        "description": "Собери все события в массив → сохрани в файл или отправь во внешнюю систему (SIEM, таблицы)"
      }
    ]
  }
]