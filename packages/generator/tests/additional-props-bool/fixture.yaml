openapi: 3.0.0
info:
  title: Test API — additionalProperties true
  version: 1.0.0
paths: {}
components:
  schemas:
    Metadata:
      type: object
      additionalProperties: true
      description: Free-form metadata dictionary

    Event:
      type: object
      required:
        - id
        - type
      properties:
        id:
          type: integer
          format: int32
        type:
          type: string
        metadata:
          type: object
          additionalProperties: true
          description: Event metadata as free-form dict
