{
  "Client_Init": {
    "usage": "let client = PachcaClient(token: \"YOUR_TOKEN\")",
    "imports": [
      "PachcaClient"
    ]
  },
  "SearchOperations_searchMessages": {
    "usage": "let chatIds = [123]\nlet userIds = [123]\nlet response = try await client.search.searchMessages(query: \"example\", chatIds: chatIds, userIds: userIds, limit: 123, cursor: \"example\")",
    "output": "SearchMessagesResponse(data: [MessageResult], meta: PaginationMeta)"
  }
}
