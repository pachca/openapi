{
  "name": "@pachca/go",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "true",
    "generate": "bun ../../packages/generator/bin/generator.ts --spec ../../packages/spec/openapi.yaml --output . --lang go && mv go/* generated/ && rmdir go"
  },
  "devDependencies": {
    "@pachca/spec": "workspace:*"
  }
}
