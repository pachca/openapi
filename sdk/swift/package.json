{
  "name": "@pachca/swift",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "true",
    "generate": "bun ../../packages/generator/bin/generator.ts --spec ../../packages/spec/openapi.yaml --output . --lang swift --examples && rm -rf generated/Sources/Pachca/GeneratedSources && mkdir -p generated/Sources/Pachca/GeneratedSources && mv swift/* generated/Sources/Pachca/GeneratedSources/ && rmdir swift",
    "build": "swift build"
  },
  "devDependencies": {
    "@pachca/spec": "workspace:*"
  }
}
